name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (leave empty to use package.json version)"
        required: false
        type: string
      prerelease:
        description: "Mark as pre-release"
        required: false
        default: false
        type: boolean
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write # for npm provenance

env:
  MAPLIBRE_FILE_NAME_PREFIX: geoman-io-maplibre-geoman-free
  MAPBOX_FILE_NAME_PREFIX: geoman-io-mapbox-geoman-free
  NODE_VERSION: "24"

jobs:
  # Validate release
  validate:
    name: Validate Release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      is-prerelease: ${{ steps.check-prerelease.outputs.is-prerelease }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            if [[ -n "${{ github.event.inputs.version }}" ]]; then
              VERSION="${{ github.event.inputs.version }}"
            else
              VERSION=$(node -p "require('./package.json').version")
            fi
          else
            VERSION=${GITHUB_REF#refs/tags/v}
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Release version: $VERSION"

      - name: Validate version format
        run: |
          if [[ ! "${{ steps.version.outputs.version }}" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-.*)?$ ]]; then
            echo "Invalid version format: ${{ steps.version.outputs.version }}"
            exit 1
          fi

      - name: Check if version exists in package.json
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          if [[ "${{ steps.version.outputs.version }}" != "$PACKAGE_VERSION" ]] && [[ "${{ github.event_name }}" == "push" ]]; then
            echo "Tag version (${{ steps.version.outputs.version }}) does not match package.json version ($PACKAGE_VERSION)"
            exit 1
          fi

      - name: Check workspace package versions
        run: |
          ROOT_VERSION=$(node -p "require('./package.json').version")
          MAPLIBRE_VERSION=$(node -p "require('./packages/maplibre/package.json').version")
          MAPBOX_VERSION=$(node -p "require('./packages/mapbox/package.json').version")
          if [[ "$ROOT_VERSION" != "$MAPLIBRE_VERSION" || "$ROOT_VERSION" != "$MAPBOX_VERSION" ]]; then
            echo "Version mismatch across package manifests (root=$ROOT_VERSION, maplibre=$MAPLIBRE_VERSION, mapbox=$MAPBOX_VERSION)"
            exit 1
          fi

      - name: Workspace validation
        run: npm run workspace:validate

      - name: Check for pre-release
        id: check-prerelease
        run: |
          if [[ "${{ github.event.inputs.prerelease }}" == "true" ]] || [[ "${{ steps.version.outputs.version }}" =~ -[a-zA-Z] ]]; then
            echo "is-prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "is-prerelease=false" >> $GITHUB_OUTPUT
          fi

      - name: Check if tag already exists
        if: github.event_name == 'workflow_dispatch'
        run: |
          if git rev-parse "v${{ steps.version.outputs.version }}" >/dev/null 2>&1; then
            echo "Tag v${{ steps.version.outputs.version }} already exists"
            exit 1
          fi

  # Build and test
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: |
          npm run typecheck:all
          npm run lint:all
          # Add test run here when uncommented in CI

      - name: Build and package (MapLibre)
        run: npm run pack -w @geoman-io/maplibre-geoman-free

      - name: Build and package (Mapbox)
        run: npm run pack -w @geoman-io/mapbox-geoman-free

      - name: API surface checks
        run: npm run api:check

      - name: Verify packaged artifacts (isolated variants)
        run: |
          test -f packages/maplibre/dist/maplibre-geoman.es.js
          test -f packages/maplibre/dist/maplibre-geoman.umd.js
          test -f packages/maplibre/dist/maplibre-geoman.d.ts
          test ! -f packages/maplibre/dist/mapbox-geoman.es.js
          test ! -f packages/maplibre/dist/mapbox-geoman.umd.js
          test ! -f packages/maplibre/dist/mapbox-geoman.d.ts
          test -f packages/mapbox/dist/mapbox-geoman.es.js
          test -f packages/mapbox/dist/mapbox-geoman.umd.js
          test -f packages/mapbox/dist/mapbox-geoman.d.ts
          test ! -f packages/mapbox/dist/maplibre-geoman.es.js
          test ! -f packages/mapbox/dist/maplibre-geoman.umd.js
          test ! -f packages/mapbox/dist/maplibre-geoman.d.ts
          test -f packages/maplibre/${{ env.MAPLIBRE_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz
          test -f packages/mapbox/${{ env.MAPBOX_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz
          test "$(node -p "require('./packages/maplibre/package.json').name")" = "@geoman-io/maplibre-geoman-free"
          test "$(node -p "require('./packages/mapbox/package.json').name")" = "@geoman-io/mapbox-geoman-free"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v5
        with:
          name: release-artifacts
          path: |
            packages/maplibre/dist/
            packages/mapbox/dist/
            packages/maplibre/package.json
            packages/mapbox/package.json
            packages/maplibre/${{ env.MAPLIBRE_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz
            packages/mapbox/${{ env.MAPBOX_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz
            LICENSE
          retention-days: 7

  # Create GitHub release
  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [validate, build-and-test]
    environment: release

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download release artifacts
        uses: actions/download-artifact@v7
        with:
          name: release-artifacts
          path: ./

      - name: Extract release notes from CHANGELOG.md
        id: release-notes
        uses: ffurrer2/extract-release-notes@v2

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: v${{ needs.validate.outputs.version }}
          tag_name: v${{ needs.validate.outputs.version }}
          token: ${{ github.token }}
          prerelease: ${{ needs.validate.outputs.is-prerelease == 'true' }}
          body: ${{ steps.release-notes.outputs.release_notes }}
          files: |
            packages/maplibre/${{ env.MAPLIBRE_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz
            packages/mapbox/${{ env.MAPBOX_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz
            LICENSE
            packages/mapbox/dist/mapbox-geoman.es.js
            packages/mapbox/dist/mapbox-geoman.umd.js
            packages/mapbox/dist/mapbox-geoman.css
            packages/mapbox/dist/mapbox-geoman.d.ts
            packages/maplibre/dist/maplibre-geoman.es.js
            packages/maplibre/dist/maplibre-geoman.umd.js
            packages/maplibre/dist/maplibre-geoman.css
            packages/maplibre/dist/maplibre-geoman.d.ts

  # Publish MapLibre package to NPM
  npm-publish-maplibre:
    name: Publish MapLibre to NPM
    runs-on: ubuntu-latest
    needs: [validate, build-and-test]
    environment: release
    if: needs.validate.outputs.is-prerelease == 'false' || contains(github.event.inputs.prerelease, 'true')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          registry-url: "https://registry.npmjs.org"

      - name: Download release artifacts
        uses: actions/download-artifact@v7
        with:
          name: release-artifacts
          path: ./

      - name: Remove npm package tarball
        run: rm -f packages/maplibre/${{ env.MAPLIBRE_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz

      - name: Publish MapLibre to NPM
        run: |
          if [[ "${{ needs.validate.outputs.is-prerelease }}" == "true" ]]; then
            cd packages/maplibre && npm publish --tag beta --provenance --access public --ignore-scripts
          else
            cd packages/maplibre && npm publish --provenance --access public --ignore-scripts
          fi

  # Publish Mapbox package to NPM
  npm-publish-mapbox:
    name: Publish Mapbox to NPM
    runs-on: ubuntu-latest
    needs: [validate, build-and-test]
    environment: release
    if: needs.validate.outputs.is-prerelease == 'false' || contains(github.event.inputs.prerelease, 'true')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          registry-url: "https://registry.npmjs.org"

      - name: Download release artifacts
        uses: actions/download-artifact@v7
        with:
          name: release-artifacts
          path: ./

      - name: Remove npm package tarball
        run: rm -f packages/mapbox/${{ env.MAPBOX_FILE_NAME_PREFIX }}-${{ needs.validate.outputs.version }}.tgz

      - name: Publish Mapbox to NPM
        run: |
          if [[ "${{ needs.validate.outputs.is-prerelease }}" == "true" ]]; then
            cd packages/mapbox && npm publish --tag beta --provenance --access public --ignore-scripts
          else
            cd packages/mapbox && npm publish --provenance --access public --ignore-scripts
          fi
