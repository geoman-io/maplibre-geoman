{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "package-lock.json",
    "turbo.json"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "dist/**",
        "packages/maplibre/dist/**",
        "packages/mapbox/dist/**",
        "packages/maplibre/*.tgz",
        "packages/mapbox/*.tgz"
      ]
    },
    "build:mapbox": {
      "outputs": [
        "dist/**",
        "packages/mapbox/dist/**",
        "packages/mapbox/*.tgz"
      ]
    },
    "build:types": {
      "outputs": [
        "dist/*.d.ts"
      ]
    },
    "build:types:mapbox": {
      "outputs": [
        "dist/*.d.ts"
      ]
    },
    "ts": {
      "outputs": []
    },
    "ts:mapbox": {
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "cache": false,
      "outputs": [
        "playwright-report/**",
        "test-results/**"
      ]
    }
  }
}
