// MapLibre GL JS's Map.on/once/off methods accept arbitrary string event names,
// so custom Geoman events (e.g. 'gm:loaded') work without module augmentation.
// This file remains as the variant counterpart to the Mapbox no-op module.
