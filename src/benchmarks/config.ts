import type { LngLatTuple } from '@/types';

export const france: LngLatTuple = [5, 46];

export const radius = 15;
export const circleSegments = 10000;
export const sourceId = 'base';
export const shapeMarkersSourceId = 'shape-markers';

export const step = 0.2;
export const size = 0.15;

export const MAX_DIFF_ITEMS = 5000;
